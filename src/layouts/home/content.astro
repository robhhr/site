---
const {title} = Astro.props
---

<div>
  {
    title && (
      <div class="mb-4 flex items-end">
        <h2 class="mr-2">recent {title}</h2>
        <a href={`/${title}`}>(all {title})</a>
      </div>
    )
  }
  <slot />
  <span class="mt-10 inline-block w-10 border-4"></span>
</div>
