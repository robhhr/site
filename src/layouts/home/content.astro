---
const {title} = Astro.props
---

<div>
  {
    title && (
      <div class="flex justify-between">
        <h2 class="mb-4">recent {title}</h2>
        <a href={`/${title}`}>see all</a>
      </div>
    )
  }
  <slot />
  <span class="mt-10 inline-block w-10 border-4"></span>
</div>
